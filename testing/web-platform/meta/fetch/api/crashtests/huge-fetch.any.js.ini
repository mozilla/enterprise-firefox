[huge-fetch.any.sharedworker.html]
  expected:
    if (processor == "x86") and debug: [CRASH, OK]

[huge-fetch.any.worker.html]
  expected:
    if (os == "linux") and (processor == "x86"): [OK, CRASH]
    if (os == "win") and (processor == "x86"): ERROR
    if (os == "android") and debug: [OK, ERROR, CRASH]
    if (os == "android") and not debug: [OK, CRASH, ERROR]

[huge-fetch.any.worker.html]
  expected:
    if os == "android": [OK, ERROR, CRASH]

[huge-fetch.any.serviceworker.html]
  max-asserts: 2
  expected:
    if (os == "android") and not debug: [OK, ERROR]

[huge-fetch.any.html]
  expected:
    if (os == "win") and (processor == "x86"): [OK, CRASH]
    if os == "mac": [OK, CRASH]
    if os == "android": [OK, ERROR, CRASH]
